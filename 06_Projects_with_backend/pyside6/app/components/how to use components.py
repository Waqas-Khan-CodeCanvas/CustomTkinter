# from app.components.buttons import PrimaryButton, DangerButton
# from app.components.forms import LabeledEntry
# from app.components.tables import SimpleTable
# from app.components.modals import ConfirmModal, AlertModal
# self.name_input = LabeledEntry(self, "Name")
# self.name_input.pack(pady=5)

# self.create_btn = PrimaryButton(self, "Create", command=self.create_item)
# self.create_btn.pack(pady=10)

# self.table = SimpleTable(self)
# self.table.pack(pady=20)
